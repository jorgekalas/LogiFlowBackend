extends layout

block content
  .text-center.mt-5
    h1.display-5.text-danger Error #{status || 500}
    p.lead.mt-3 #{message || "Ocurrió un error inesperado."}
    a.btn.btn-primary.mt-4(href="/") Volver al inicio
